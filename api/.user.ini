; Lido pelo PHP-CGI/FastCGI (cache de ~5 min — user_ini.cache_ttl).
; Importação SINAPI: os 4 XLSX oficiais do CEF somam ~17 MB num único POST;
; os padrões do PHP (upload 2M / post 8M) rejeitariam o envio silenciosamente.
upload_max_filesize = 32M
post_max_size = 64M
max_file_uploads = 20
